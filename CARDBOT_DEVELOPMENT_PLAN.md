# CardBot 比赛开发计划

> 历史阶段计划，当前实施状态以 docs/STATUS.md 为准；规划和建议不能作为已接通 API 的证据。2026-09-12 新界面是独立浏览器演示，真实 CRM 位于 /crm.html。

## 1. 产品定位

CardBot 是面向外贸销售团队的小型 AI 工作台。第一阶段只完成两条互相衔接的闭环：

1. 同一批任务从早间待办贯穿到晚间复盘。
2. 历史邮件形成事实依据，经人工审核生成本地草稿，再交付到小满草稿箱。

AI 不直接发送邮件。新客户自动获客、一键安装和完整视觉升级排在核心闭环之后。

## 当前开发进度（2026-09-11）

- 已完成：CardBot 品牌基线与旧品牌残留清理。
- 已完成：同一批任务的早盘固定、日间状态、完成依据、受阻原因、晚盘复核、个人/团队同源视图。
- 已完成：历史邮件事实引用、人工通过/驳回、本地持久化草稿的核心服务。
- 已完成：统一小满连接器骨架；Mock 可完整保存，官方 API 与 Playwright RPA 会在缺少参数或账号时明确返回“待配置”。
- 待完成：EML 批量导入与附件解析、DeepSeek 自动提取事实和生成草稿、真实小满账号校准与重登复核。
- 按计划后置：智能获客接入、一键安装/启动/测试和正式 UI 视觉升级。

## 2. 品牌与代码约束

- 产品名、页面标题、提示词、接口协议、包名、环境变量、数据库名、测试账号和构建日志统一使用 CardBot/cardbot/CARDBOT。
- 新工作目录不得出现旧品牌字符串，构建产物也必须通过残留扫描。
- 本轮 UI 只做品牌替换；布局、配色、组件细节等视觉改版等待单独设计要求。

## 3. 总体结构

```text
个人端 / 管理端
       ↓
CardBot 业务服务
  ├─ 全天任务闭环
  ├─ 邮件事实与审核
  ├─ 本地草稿与版本
  └─ 统一小满连接器
       ├─ Mock
       ├─ 官方 API
       └─ Playwright RPA
       ↓
小满草稿箱
```

模型通过统一 AI Provider 调用。DeepSeek 采用 OpenAI 兼容协议，当前先保留配置，不依赖真实密钥开发业务流程。

## 4. 分阶段实施

### 阶段 0：CardBot 基线

目标：建立可以继续开发、可以验证且不含旧品牌的代码基线。

- 完成全量品牌替换和文件名调整。
- 清理旧构建产物，重新安装依赖并构建。
- 修正 Windows 测试脚本，形成品牌残留扫描。
- 建立比赛演示数据，不使用可能属于真实域名的测试邮箱。

验收：源码、配置、测试和新构建产物中旧品牌字符串为 0；应用可启动并通过健康检查。

### 阶段 1：同一批任务贯穿早晚

目标：先跑通工作助手硬性验收链路。

- 新增统一任务、任务来源、状态事件、完成依据四类数据。
- 从客户邮件、上级安排、跨部门消息生成或合并任务。
- 个人端查看并更新自己的任务；管理端读取同一份任务总账。
- 保存早间快照，晚间必须针对早间同一组任务核验。
- 没有期限、负责人或完成证据时显示“待确认”。

验收：演示“信息来源 → 早间清单 → 白天状态变化 → 晚间提醒”，前后任务 ID 一致，个人端和管理端状态一致。

### 阶段 2：历史邮件、事实、审核、本地草稿

目标：在不依赖小满真实账号的情况下跑通销售助手主体。

- 导入脱敏 EML/JSON 邮件和产品资料。
- 建立邮件会话、消息、附件、事实、来源引用和冲突记录。
- DeepSeek 输出结构化的客户诉求、已确认事实、待确认项和草稿。
- 建立草稿版本、关键句依据、风险标记和人工审核记录。
- 价格、交期、赔偿等敏感承诺必须强制审核。

验收：打开任意关键事实能回到原始邮件；草稿保存后可回读；本地草稿不会冒充小满草稿。

### 阶段 3：统一小满连接器与 Mock

目标：业务代码只认识统一接口，不被小满接入方式绑死。

- 定义能力探测、读取邮件、保存草稿、查询草稿、连接状态和错误模型。
- 完成 Mock 连接器，模拟成功、权限不足、登录失效、超时和结果不确定。
- 记录幂等键和外部操作证据，避免重复保存。

验收：三种连接器使用同一套契约测试；切换模式不改业务代码。

### 阶段 4：真实小满 API / RPA

前提：主办方提供测试账号，并确认 API 权限或允许 RPA。

- API 路线：接入 client_id/client_secret，先做能力探测，再实现公开且已授权的能力。
- RPA 路线：保存登录状态，定位客户邮件和草稿箱，保存草稿后回读核对。
- 对验证码、登录过期、页面变化和保存结果不确定提供可恢复状态。
- 完成首次保存、草稿箱复核、退出重登复核。

验收：重新登录小满后仍可找到收件人、主题、正文一致的草稿，并有截图或外部定位作为证据。

### 阶段 5：接入现有智能获客

目标：覆盖新客户开发场景，同时复用前面的事实、审核和小满交付链路。

- 从现有获客结果选择已核验客户，不重做搜索引擎。
- 保留公司、网站、邮箱和信息来源；缺失联系方式不得编造。
- 生成合作切口和开发信，继续走事实检查、人工审核和小满草稿保存。

验收：候选来源可追溯，开发信不含虚构事实，草稿在小满真实可查。

### 阶段 6：一键安装、启动、测试与演示包装

- Windows 一键启动和健康检查。
- 一键写入演示数据、一键运行自动测试、一键清理演示数据。
- 比赛演示脚本覆盖正常、待确认和小满保存失败三种情形。
- 最后完成正式 UI 视觉和交互细节。

## 5. 开源项目选择

| 用途 | 决策 | 原因 |
|---|---|---|
| 小满网页自动化 | 复用 Playwright | 项目已经安装，适合保存登录状态、定位页面、截图和端到端测试。 |
| EML 邮件解析 | 新增 PostalMime | 体积小、无外部依赖、支持 Node.js/TypeScript，可解析收发件人、正文和附件。 |
| 早晚任务和后台重试 | 复用 BullMQ | 项目已经使用；适合延时任务、重试和幂等执行。开发模式保留无 Redis 的本地执行后端。 |
| AI 调用观测 | Langfuse 暂缓 | 有价值但部署较重；核心闭环跑通后再决定是否接入，比赛首版先做本地模型调用日志。 |
| Agent 编排 | 不引入 LangChain/Flowise | 现有代码已有 Agent Runtime、工具权限和技能选择，再引入一套编排层会形成两套状态和排错链路。 |
| 自动化工作流平台 | 不引入 n8n | 核心数据、审核和验收证据必须留在 CardBot，外置工作流会增加部署与状态一致性风险。 |
| 小满 API SDK | 自建薄连接器 | 官方接口简单，且邮件草稿能力尚未确认；自建小型适配层更容易同时支持 API、Mock 和 RPA。 |

所有新增依赖在加入前检查许可证，并写入第三方声明。不开源项目整套搬运，只引入经过契约测试的库。

## 6. 业务技能

现有技能继续保留：系统说明、咨询、客户与商机、自动获客、开发信与触达、贸易单据。

本轮新增：

- `daily-work-loop`：保证同一批任务贯穿早晚，并用完成依据判定状态。
- `evidence-email-drafting`：先提取事实和不确定项，再生成可追溯草稿。
- `okki-draft-delivery`：规范 Mock/API/RPA 的选择、保存、回读和重新登录复核。

连接器和模型 Provider 是代码接口，不伪装成技能；技能只表达业务判断与完成标准。

## 7. 主办方待确认事项

- 测试账号是否同时开通 API 对接权限。
- 是否提供 client_id/client_secret。
- 是否有读取完整历史邮件和附件的 API。
- 是否有新建、查询邮件草稿的 API。
- 若没有邮件草稿 API，是否允许 Playwright/RPA。
- 登录是否包含验证码、短信或扫码，测试环境是否提供稳定登录方式。

这些问题只决定阶段 4 采用 API 还是 RPA，不阻塞阶段 0 至阶段 3。
