# 当前开发状态 — 2026-09-12

这是接手时的事实基线。`已实现`、`测试通过`、`真实外部系统验证`是不同层级。后续开发每次交付更新此页；不要用百分比掩盖未接通的链路。

## 结论

当前处于：**已有后端基础 + 左侧导航的交互工作台 + 可播放的七步模拟业务演练 + 个人扑克牌 Bot/知识助手演示；尚未完成真实小满业务闭环或真实企业知识库接入。**

### 最新交付：个人 CardBot 与知识助手演示

- CardBot logo 右侧新增扑克牌 Bot，不增加侧栏宽度和品牌栏高度。Bot 使用固定 1:1.4 扑克牌外形，以轻微眼神移动与眨眼提供生命感；低动态模式停止动画。
- 首次点击打开小型选择器，可选 16 种表情与 12 种颜色；配置按 `sales-01`、`sales-02`、`sales-03`、`manager` 四个演示账号分别保存在 localStorage，账号切换不会串号。
- 保存后点击 Bot 打开知识助手演示；支持预置问题、自行输入、最小化、恢复、关闭和重新定制，并适配中英文、深浅主题与移动端。
- 当前回答是本地关键字规则与固定文案：不接企业知识库、不调用 DeepSeek、不上传对话，也不会写入小满。正式 RAG 需要企业批准资料、租户/角色权限、引用、版本与审计后另行实现。
- 视觉目录参考 `opc8838-hub/bot` 编辑器的扑克牌形体、12 色与 16 表情；工作台使用独立轻量实现，已补 MIT 许可与第三方说明。
- 本次新增浏览器验收：选择数量、保存、账号隔离、对话回答、缩小/恢复、主题/语言、无横向溢出及 JS page error；生成 `v4-bot-picker.png`、`v4-bot-chat.png`、`v4-bot-chat-dark-en.png` 本地截图。
- 本次通过 `npm run test:core`、`npm run build --workspace frontend`、`npm run test:preview-ui`。前端构建仍有既存大 chunk 警告；真实模型、小满和知识库不在本次验证范围。

### 此前交付：旧界面留底与工作台布局微调

- `archives/cardbot-visual-v2.html` 保存工作台重构前 `c3caa2f` 版本；单个 HTML 内嵌样式、脚本和地球数据，双击离线可用。浏览器数据使用独立前缀，不混入当前演示记录。旧 CRM 链接只提示离线限制，不连接真实系统。
- 左侧导航整体、右侧主区域整体增加桌面 20px / 手机 16px 圆角；缩小左右留白、取消内容最大宽度，保留当前字号和业务逻辑。
- 当前工作台删除原 CRM 导航链接；旧 CRM 文件和登录边界不变。
- 顶部工具栏已吸顶固定；滚动时中英文、深浅主题和演示账号切换持续可用。演练过程中切换账号会先退出独立演练快照，再进入所选账号总览；这仍是 Demo 身份，不是真实登录。
- 总览的“播放演练”入口缩短高度并减少下间距，使下方任务与地球区域上移；没有缩小其说明字号。
- 本次通过：核心测试、前端构建、工作台浏览器验收，以及新增 `python frontend/tests/cardbot_archive_smoke.py` 离线留底验收（零外部请求、主题/语言/草稿持久化）。人工检查桌面深浅色、手机菜单及旧版留底截图。
- 未新增真实 API 验证；后续仍按 ROADMAP 推进后端绑定和授权确认。视觉调整待用户复核。

### 已有交付：工作台与模拟接入演练

- 总览、今日工作、客户与邮件、草稿与审核、模拟小满草稿箱、团队进度、企业设置组成应用工作台；地球放在总览，业务页不重复品牌首屏。
- 点击“播放演练”按 7 秒/步依次展示早间、邮件依据、预置拟稿、模拟人工审核、模拟保存读回、晚间、经理汇总。支持暂停、前后跳步、重播、退出；页面隐藏时不推进步骤。
- 演练快照与手动 localStorage 数据分开；倒退会恢复对应步骤；退出恢复原身份和手动数据。刷新退出演练，保留手动记录。
- 一位业务员 Jojo 拥有同一批三项任务；Mina 和 Leo 暂无任务。经理汇总 2/3 完成时报价仍待确认。
- 模拟草稿箱显示 SIM-OKKI-DRAFT-001 与源正文；这是本地模拟回执，不是 API 请求或真实读回证据。预置回复也不是 DeepSeek 生成。
- 手动修改正文撤销审核并清除原模拟回执；未审不可保存；模拟保存失败仍保留正文。
- 界面不请求外部业务 API；未新增真实账号登录、后端多人同步或真实 RPA。

本次优先解决用户明确反馈：增强主问候三行之间的呼吸感、下移说明和按钮、统一提高中英文字重，并显著放大地球、指标、任务与详情中的小字；同时先做企业管理员/业务员身份衔接 Demo。

## 已实现且本次验证

| 项目 | 实际实现 | 验证范围 |
| --- | --- | --- |
| 多语言问候 | Hello 先停 1.0 秒，后续各语言淡出 0.28 秒/停留 0.85 秒，总约 6.7 秒 | 浏览器实测；可跳过；减少动态模式不自动切换 |
| 中英文切换 | 开场、功能页和工作台一键切换，语言本地记忆 | 标题、导航、任务、依据、草稿与接入说明均纳入自动化验收；业务原文不改写 |
| 上海时区问候 | 06:00–13:59 早上、14:00–17:59 下午、18:00–05:59 晚上；中英文同步 | 6 个边界时间单元测试；中文主标题无句号 |
| CARD 背景 | 离屏文字遮罩采样；小词深浅构成巨型 CARD | 桌面/手机截图人工查看；不是截图贴图 |
| 功能页 | Evidence / Workday / Review / Draft / Deliver；不含登录表单 | 5 词 DOM 检查；手动进入工作台，不计时赶走用户 |
| 新工作台 | 左侧功能导航，右侧业务内容；播放演练入口 | 浏览器操作、真实业务状态变化与独立演练快照 |
| 文字地球 | world-atlas 大陆数据 + Canvas 投影，小字构成陆地；提高陆地文字、轮廓与城市标签对比度 | 旋转、拖拽/方向键、浅/深色、手机视图 |
| 小字可读性 | 业务说明 12–15px；紧凑地球词最小 10px并按画布调整密度 | 桌面与移动端截图人工检查 |
| 时间坐标 | 地球旁 6 个常显全球时区；11 个市场可选；时钟、UTC 偏移和夏令时实时计算 | 城市选择本地记忆；坐标不是客户实际位置 |
| 同批早晚任务 | 3 条稳定 ID；晚间汇总读取同一份任务 | 状态与完成依据保存、个人/团队一致 |
| 企业多身份 Demo | 1 位经理 + 3 位业务员；虚构 CardBot ID 映射虚构 OKKI user_id | Jojo 的完整三项任务；其他业务员空状态；经理看同一批汇总 |
| 草稿本地预览 | 固定模板 → 勾选核对 → 审核 → localStorage 保存 | 刷新持久化；编辑后重新审核；保存不自动完成报价任务 |
| 后端工作助手 | workday 领域逻辑及 HTTP 接口 | `workday-test.ts`、`workday-http-test.ts` 本次通过 |
| 后端邮件证据 | PostalMime EML 解析、引用事实字段 | `email-evidence-test.ts` 本次通过 |
| 后端本地草稿/Mock | JSON 文件仓储、审核、本地/Mock receipt | `email-draft-workflow-test.ts` 本次通过 |

## 测试记录

- `npm run test:core`：通过。含预览基础断言、演练同批任务/倒退/隔离/模拟回执审核门槛、原前端 124 项检查及后端核心测试。
- `npm run build --workspace frontend`：通过。产出两个页面 `index.html` 和 `crm.html`。原 CRM 仍有 >500KB chunk 警告，未当作已优化。
- `python frontend/tests/cardbot_preview_smoke.py`：通过。覆盖主题/语言、引用跳转、审核、模拟保存失败与持久化、编辑撤销、演练自动推进/暂停/倒退/隔离、经理汇总和多尺寸布局。
- 浏览器尺寸：1440×1000、390×940、768×940、1920×940；Chromium。截图在本机 `frontend/tests/artifacts/`，不提交生成物。
- 已在授权登录会话中只读核验小满工作台：可见邮件、沟通、线索、客户、商机、交易、团队数据、协同、企业管理等模块，以及消息回复、客户跟进、数据洞察、审批等跟进任务类别。本次没有生成邮件、标记已读、保存草稿或修改客户资料。
- 已核验小满官方开放平台存在，且需主账号在“企业管理 → 外部对接 → API 对接”取得应用凭证；尚未取得本企业 API 权限，也未发现公开的邮件草稿保存/读取端点。
- 未在本次验证：真实草稿写入、草稿箱读回、退出再登录复核、全量旧业务测试、MySQL/Redis/WhatsApp 集成、外网部署、Safari/Firefox、真实模型调用、真实邮箱读取。
- 测试套件验证的是模拟/本地事实，不是赛事要求的远端持久化证据。
- GitHub Actions 已从失效的 Webpack/master 配置改为 main 分支的 Node 24、锁文件安装、核心测试和前端构建。云端 Actions 结果需在首次 push 后查看，不以本地通过代替云端结论。
- 首次项目源码/文档已于本次交付推送到 `opc8838-hub/cardbot` 的 main 分支，保留远端原有 README 初始提交历史。未上传账号说明、个人业务表、node_modules、生成产物和本地业务数据。
- 发布前模式扫描未发现真实 token/私钥形态或旧品牌文本；10 处数据库 URL 命中经检查为示例/测试/变量模板。此检查不是完整安全审计。
- 初始代码导入的 `git diff --cached --check` 报告旧文件末尾空行/空白与历史 Markdown 换行；本次未为了发布重写无关文件。这不影响已运行的功能测试。

## 明确尚未完成

1. 新视觉工作台与真实后端 API 绑定。当前新页面的状态来自独立 `preview-store.ts`，与 `/crm.html` 不共享业务数据。
2. 新登录页视觉。用户要求另行设计；旧 CRM 仍可直接访问 `/crm.html`，新工作台不再展示该链接。
3. DeepSeek 实际接入和端到端事实约束。既有运行时可发 OpenAI 兼容请求，但未配置本项目真实账号，也未把生成链路接入新 UI。
4. IMAP 历史邮件读取。ImapFlow 与 mailparser **未引入**；现有 EML 使用 PostalMime。不要照早期建议表写成全部已采用。
5. 小满企业主账号的 API 对接权限与 `client_id/client_secret`。官方鉴权协议已确认，但当前企业是否开通、赛事账号是否允许使用仍待主办方确认。
6. 小满草稿读取比对、草稿箱 UI 复核、重新登录复核。连接器当前没有 readDraft/verifyDraft 方法。
7. Playwright 小满页面驱动。只有可注入驱动的骨架，不是已完成 RPA。
8. 新客户开发与上述草稿链路的整合。原获客模块保留，不代表本次已接入。
9. 多人跨设备同步、生产鉴权/租户隔离、安全审计、幂等和可靠重试的完整验收。

企业多身份页面是浏览器端交互 Demo，不是生产安全边界：尚无 CardBot 服务端登录、角色表、企业租户表、用户绑定审批或小满真实 `user_id`。当前全部身份与 ID 均为虚构演示数据。

## 接入层的真实能力

- Mock：能产生模拟保存回执，不能登录小满查到，不具有远端保存证明力。
- 官方 API：小满开放平台和 OAuth 取 token 方式已确认；现有代码仍只是按配置执行 Bearer POST 的通用适配器，尚未实现凭证换 token、刷新和业务端点映射。公开权限列表含客户、线索、商机、用户、仓库、报表、Webhook 等，未看到邮件草稿权限；不能据此宣称草稿 API 可用。
- RPA：没有实际页面驱动时返回未就绪；需要授权账号、允许自动化的确认、选择器与独立 Worker。
- 后端本地草稿：落 JSON 文件；不是多实例事务数据库。并发写、版本历史、远端失败补偿与审计仍需完善。

## 已识别的技术债与风险

- 旧 `crm.html` 和 `prototype-api.ts` 很大；需要按模块渐进拆分，不能一次重写全部 CRM。
- 前后端存在两套阶段实现，未来必须建立适配层及统一领域契约；不能把演示数据悄悄当真实业务数据。
- 旧 deliver 路由需审计外部调用前的审核校验、Mock 回执标识和任务完成语义；在这项审计完成前不得打开真实小满写权限。
- 当前预览人工完成只校验依据非空；不是核验第三方证明，管理视图也不是多人实时协作。
- 本地演示数据可被浏览器开发工具修改，不作为审计证据。
- 全球城市坐标是内置参考；尚未做真实客户地址标准化/地理编码/隐私授权。
- 历史文档中的“已验收”属于历史描述，不自动计入本次验证。
