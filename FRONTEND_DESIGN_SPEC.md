# CardBot 前端设计规范 v3

日期：2026-09-13。状态：新预览已实现，待用户视觉复核；不是旧 CRM 全部页面完成改版。

## v3 工作台规范（优先于下方 v2 首屏尺寸）

- 左侧 224px 固定功能栏，右侧顶栏、状态说明及业务内容。低于 700px 时左栏改为可展开菜单。业务页切换不再重复展示大号地球首屏。
- 七个区域：总览、今日工作、客户与邮件、草稿与审核、模拟小满草稿箱、团队进度、企业设置；后两者面向经理演示身份。
- 总览保留上海时间问候、原创文字地球、六个世界时钟与市场选择；主标题 32px（移动端 27px），业务正文 14–15px，说明 12–13px，局部等宽刻度最小 11px。地球按缩小后的半径重采样，字体最小 10px。
- 内容面板圆角 17px，操作按钮保持胶囊与黑白主色；紫色仅用于待确认、引用和演练进度。渠道品牌 Logo 与用户自选 Bot 颜色不受系统强调色替换影响。
- 整个左侧栏及右侧主区域均为独立圆角容器：桌面 20px、手机 16px；外边距与两栏间距 10px，手机外边距 8px。左栏随滚动固定，手机展开菜单也保留圆角。
- 内容不再限制 1780px 最大宽度；左右内边距桌面 20px、中等屏 18px、手机 16px。大屏不再额外加大至 44px，让业务面板铺满可用空间；不通过缩小字号实现。
- 工作台移除“原 CRM 独立入口”；仅移除导航链接，不删除 `/crm.html` 页面或修改其鉴权。旧版留底文件见 `archives/cardbot-visual-v2.html`，不作为当前工作台设计依据。
- 顶部工具栏在页面滚动时吸顶到视口 `top: 0`，使用完全不透明的主题背景，禁止滚动内容从顶部缝隙或工具栏本身透出；四角均保留桌面 19px / 手机 15px 圆角。语言、主题与演示账号切换始终可见可操作。演练中切换账号会明确退出演练，回到该账号总览，避免把固定剧本身份误认为用户本人。
- 总览的演练入口采用紧凑横条：桌面 12px 纵向内边距、38px 播放图标、18px 下间距；其下业务双栏随之上移，正文仍保持 13px。
- “播放演练”常驻总览入口，业务页也可启动。播放器显示当前步骤、虚构业务时间、说明和前后/播放/暂停/重播/退出操作。每步 7 秒，不自动对业务内容执行真实操作。播放后当前进度点使用实紫，走过的步骤为浅紫；当前功能导航和页面核心面板同步点亮，步骤变化时焦点按顺序迁移，并始终只保留一个主焦点。
- 演练期间审核与完成按钮不可手动改写快照；退出恢复原先手动编辑的数据。模拟草稿箱始终有显著模拟标识，不仿冒真实小满站点。
- 下方涉及旧巨型问候行距的规则仅作旧版历史参考；以本节和 `workbench.css` 为当前应用工作台规范。

## 体验顺序

完整有声开场以 `frontend/motion/cardbot-entry/build/timeline.json` 为唯一时间事实源，总长固定 10.000 秒；音乐使用本地 AAC 资源，不依赖网络音频服务，也不通过改变播放速度硬凑时长。

1. 黑底多语言问候：Hello → 你好 → Hola → Bonjour → مرحباً → こんにちは。首词停 0.75s；后续淡出 0.17s、停 0.64s，总计 4.8s。首个 Hello 出现时同步启动音乐；可 Skip 或 Escape。
2. CardBot 卡牌过场：播放本地 1920×1080、60fps、3.4 秒 MP4，中央黑卡展开为多色卡牌矩阵后归位；以媒体 `ended` 事件衔接下一段，不用固定延时猜测结束。
3. 独立登录页：视频末尾中央黑卡沿 Y 轴翻到白面，再展开成白色登录卡。动画固定最终卡片尺寸，以合成层缩放和旋转完成，不动画宽高；总计 1.8 秒 / 108 帧，关键节奏帧为 0、6、96、108。账号、密码可留空并直接点击 Log in；这是入口交互，不代表已建立生产鉴权。问候、视频与翻卡合计正好 10 秒。右上角仅显示 28–30px 的扬声器图标，不附可见文字；保留 `aria-label`、键盘焦点和静音状态。浏览器阻止首屏有声播放时，图标必须反映静音事实，并在用户点击后按当前时间轴位置恢复声音。
4. 黑白功能页：登录后默认英文；小词组成巨大 **CARD** 背景，前景为 Evidence、Workday、Review、紫色 Draft、Deliver。用户手动点击 Enter workspace。
5. 交互工作台：左侧功能导航，右侧业务内容；总览保留全球文字地球与今日操作。进入时延续英文，可切换中英文、经理和三位业务员。

## 视觉方向

黑白编辑式排版，宽松留白，大字与极小等宽字形成对照。参考只用于构成方法，不复制 ANSYN 标识、文案或截图地球。大字功能保留用户指定词。

| 设计项 | 实施值 |
| --- | --- |
| 浅色主背景/表面 | #f8f8f6 / #ffffff |
| 深色主背景/表面 | #121313 / #1b1c1c |
| 浅色正文 / 深色正文 | #171819 / #f1f1ed |
| 信号紫 | #6d4aff / 深色 #ac92ff，仅少量功能强调 |
| 正文字体 | Helvetica Neue / Segoe UI / Microsoft YaHei 回退 |
| 数据小字 | Consolas / Liberation Mono / monospace，本地优先不依赖外部字体 |
| 主框圆角 | 桌面 16px、手机 12px |
| 详情面板圆角 | 20px，手机 17px |
| 按钮 | 100px 胶囊、42px 最小高度；按下轻缩放 |
| 内容间距 | 主内容 38px、手机 18–22px；细线分隔，不堆阴影 |
| 操作区标题 | 桌面 36px、手机 30px，中等字重紧字距 |
| 手机断点 | 760px；中等屏 1100px |

小字不能为了“高级感”牺牲可读性。桌面正文与说明建立硬性字号下限：任务来源与坐标说明 12px，状态与指标说明 12–13px，事实/表格说明 14px；手机主要说明不低于 11–13px。地球内部词在桌面约 11–14px，并相应降低采样密度，保证单词可辨认而不是糊成纹理。自动化测试会检查关键小字的计算字号，防止后续回退。

系统采用 CSS 变量，主题保存在 `cardbot_theme`。不是只反转地球颜色，正文、边线、输入框和按钮需一起适配。主题切换使用浏览器 View Transition 将整页作为合成层做约 580ms 的呼吸式明暗过渡，降级浏览器使用颜色渐变；不得为了切主题重建工作台 DOM。中英文按钮是真实界面切换，语言保存在 `cardbot_locale`；业务原文、客户邮件正文和证据引用不应被界面翻译篡改。

工作台主问候只按中国标准时间 `Asia/Shanghai` 判断，不跟浏览器所在地或所选市场变化：06:00–13:59 为早上 / Good morning，14:00–17:59 为下午 / Good afternoon，18:00–05:59 为晚上 / Good evening。中文三行标题不使用句号，行高为桌面 1.22、手机 1.19；标题下留白分别为 44px/36px，说明与操作按钮再留约 30–35px，形成清晰呼吸感。工作台正文默认采用中等字重，中文与英文遵循同一层级，不使用过细字重。

## CARD 小字背景

素材词库：cardbot / review / workday / draft / evidence / human / trade / source / system / today / followup / local。词库是装饰，不是实时状态。

## 空闲屏保与顶部屏保按钮

- 工作台 20 秒没有输入后，用全视口文字地球覆盖当前界面；大地球尺寸为桌面 `min(82vmin, 940px)`，手机为 `104vmin`，沿用 CARD/地球的小字词库和当前主题色。
- 屏保进入使用轻微淡入与尺度收拢，退出约 240ms；鼠标移动只用于工作台空闲计时，屏保显示后不退出。点击屏保任意位置、按键、触摸或滚动才退出，且同一次操作不得传递到底层页面。减少动态模式保留静态地球，不强制旋转。
- 顶部屏保按钮为 38×38px 纯 SVG 图形按钮，手机为 35×35px；不得增加可见文字。无障碍名称和悬停提示随中英文切换，点击立即预览大地球屏保，不进入十步业务演练。

离屏画布先绘制 CARD，再用密集小词采样遮罩；字形内深、字形外极浅，并有确定性的深浅差异。前景大词与背景分层，不能把 CARD 写成一个普通实心大标题。手机保持五个词可读、不互撞、不横向溢出。

## 文字地球

真实世界陆地轮廓来自已安装 world-atlas，地理判定由 d3-geo 完成。按经纬度取样，将词投影到球面，陆地明显、海洋极淡；球面背面剔除，边缘压缩产生立体感。慢速自转，可拖动及左右方向键。不是原图的灰色实心地球，不使用假业务数据填充装饰词。

地球自转速度约为每毫秒 `0.0047°`；球体半径为可用短边的 47.5%，在卡片内略微放大。低动态模式仍停止自转。陆地文字和球体轮廓需要比背景装饰更明确，但仍弱于左侧主标题。地球周围常显洛杉矶、纽约、伦敦、迪拜、上海、悉尼 6 个时区的实时钟和 UTC 偏移；城市名比时间和偏移高一级。市场选择器提供 11 个代表城市，选择保存在 `cardbot_market`，切换中英文后不丢失。

时间由浏览器的 `Intl` 时区数据库实时计算，自动处理夏令时；城市坐标为预置演示参考，必须有提示。未来真实客户地图应显示来源、精度、更新时间，不能把城市中心当客户精确地址。

## 操作与信息规范

- 全程可见“虚构演示/浏览器保存/不发送”边界。
- 来源在任务详情可见；待确认事实单独列出。
- 任务完成需要输入依据；草稿保存不能自动等于报价发送。
- 草稿先生成，再核对勾选、批准、保存；编辑后撤销审核及旧保存时间。
- 真实小满未连接时，显示未就绪，不用绿色成功标识或 LIVE 字样冒充。
- Team 与个人视图来自同一状态；不硬编码完成数。
- 企业 Demo 中，经理可见团队、连接和 Organization；业务员只可见自己的任务、依据与草稿。前端隐藏只是演示，生产环境必须由服务端按租户、角色和小满 `user_id` 再次授权。
- 导出仅导出虚构预览数据，不包含账号/客户库。

## 个人 CardBot 与知识助手演示

## 客户开发与现有客户双业务线

- 左侧“客户开发”和“客户与邮件”是两个业务入口，不复制两套审核系统。总览使用两条入口汇入 Research / Decide / Draft / Review / Audit 的结构表达关系。
- 客户开发固定为六阶段：定义目标、发现企业、研究背景、开发判断、寻找负责人、准备沟通。阶段栏使用既有表面、边线、17px 面板圆角和信号紫；当前阶段实心紫，已完成阶段浅紫，未到阶段降低层级但保持可读。
- 桌面客户开发内容优先三栏：目标客户、候选企业、企业档案；负责人和合作切入点使用下方整行。1250px 以下两栏，700px 以下单列。所有操作按钮继续使用 41px 以上胶囊按钮、Flex 双轴居中，不另造小号按钮体系。
- 候选排序必须先呈现匹配理由和业务信号，再呈现联系人。联系人必须带角色和来源；开发判断必须显示“为什么匹配 / 为什么是现在 / 资料依据”。
- 开发信与现有客户回复在同一“草稿与审核”页切换，共用人工核对、保存、未发送状态和审计文案；不能因为是开发信就绕过人审。
- 十步演练只保留一个紫色当前焦点。前四步依次聚焦目标、候选、企业档案、负责人和切入点，后续迁移到任务、邮件、草稿、审核、记录和团队页面。

## 视频尾帧与登录卡连续性

- 登录黑卡首帧必须承接 MP4 尾帧中央卡：视口居中、约 0.445 起始缩放、Y 轴 198°、Z 轴 -5°，并使用与视频一致的大眼比例。
- 前 6 / 108 帧保持起始几何，随后只通过合成层 transform 同时翻转和放大，白色登录面随旋转自然出现；禁止先缩小、切换尺寸或替换 DOM 后再启动。
- 事实帧、捕获脚本和连续性说明保存在 `frontend/motion/cardbot-entry/qa/`；低动态模式仍直接进入最终登录状态。

- CardBot logo 右侧放置同高量级的扑克牌 Bot，品牌行最小高度 44px；不得为 Bot 增加侧栏宽度或顶栏高度。扑克牌比例固定为 1:1.4，圆角形体不允许用户换成其他轮廓；未配置状态也不添加紫色虚线框、彩色光晕或常驻提示边框。
- 首次点击未配置 Bot 时打开小型非阻塞选择器；提供 16 种表情与 12 种颜色。每个演示账号用 `cardbot_bot_v1_<accountId>` 分别保存选择，切换账号不能串用配置。
- Bot 的生命感来自眼神轻微游移和眨眼，不做持续上下漂浮。`prefers-reduced-motion` 下停止动画。表情仍需在 24–44px 的侧栏尺寸可辨认。
- 保存后打开知识助手，开场语为“需要帮你做什么”；原菱形图标改为字形舒展的“定制 / Customize”。对话框支持定制 Bot、最小化、恢复和关闭；桌面端可抓住标题栏拖动且始终限制在视口内，手机端保持稳定的底部弹层，不启用拖拽并不得横向溢出。
- 常用企业问题横向排列，保留一条浅色、可拖动的原生横向滚动条；中英文均提供 8 个问题，覆盖报价、邮件、样品、折扣、付款、交期、认证和客户资料。点击问题或手动输入后，先出现 Thinking 状态，再输出对应的完整预置答案；回答正文不使用“演示回答”前缀。
- 缩小后的 CardBot 收成约 176×48px 的迷你浮层，只保留左侧 Bot 卡片、中间 CardBot 文字和右侧关闭键。默认位于左下角并向右留 28px；卡片和中间文字都是拖拽热区，中间文字短按仍可展开。桌面端可越过内容区放到左侧导航上方；手机端也允许移动。展开和缩小沿用最近位置，同时继续约束在可见视口内。
- Bot 对话组件使用当前 Bot 颜色作为强调色：定制、确认选择和发送按钮同步变色；奶油白、琥珀、绿色、青绿色等浅色底自动切换深色文字，其他颜色使用白字。
- 当前问答是固定规则返回，仅演示报价、邮件规范与样品政策。界面必须持续标注“未连接真实知识库 / 不调用模型 / 不上传”，不得把预置答案写成 AI 检索结果。
- 正式版应由企业批准的知识库、租户权限、来源引用与审计记录驱动；Bot 只负责交互入口，不绕过草稿审核、数据权限或外部写入门槛。
- 视觉目录参考用户的 Bot 编辑器，但当前工作台组件为轻量独立实现；第三方署名与许可见 `THIRD_PARTY_NOTICES.md`。

## 手机远程设置

- 顶部账号切换右侧常驻 42px 手机/显示器组合图标，图形采用细线轮廓；手机端缩为 35px。侧栏“小满草稿箱”不再显示额外“模拟 / SIM”尾标，但业务内容内部仍保留必要的演练边界。
- 点击后使用遮罩在视口中央打开远程设置，包含扫码入口、刷新/复制/停止控制以及 WeChat、Lark、WhatsApp 三个 Bot Channel；不得出现 Telegram。三个渠道都显示可识别的本地图形标识，不使用单个汉字或 `WA` 字母代替 logo，也不从 CDN 热链接。
- 当前二维码和 Channels 只演示未来界面，状态文字必须说明尚未接入真实跨设备会话服务。复制链接只复制当前本地预览地址，不宣称手机可从公网访问。
- 弹窗支持中英文、深浅主题、遮罩或关闭按钮退出、Escape 退出。窄屏改为单列并在弹窗内部滚动，不让页面横向溢出。

## 全渠道能力演讲分页

- 手机远程设置弹窗同时承担产品演讲入口：第一页保留二维码及微信、飞书、WhatsApp 渠道预览，后续 5 页依次展示统一能力、客户线、知识检索、任务判断与草稿审批、审计闭环。
- 所有渠道必须指向同一 CardBot 核心，并显示同一组能力：客户线、知识检索、任务判断、草稿审批、审计。不得因为渠道不同而复制或改写业务规则；平台差异只存在于消息卡片和交互格式适配层。
- 底部常驻页码、进度短线和左右圆形箭头；点击进度可跳页，桌面支持左右方向键。最后一页不循环跳回，避免演讲误操作；Escape 仍关闭弹窗。
- 每页只讲一个核心命题，正文控制在可口述长度，通过渠道汇聚图、客户时间线、检索链、任务审批管线和审计日志表达关系。桌面弹窗继续保持原有 860px 上限；移动端改为单列并允许页内纵向滚动。
- 能力总览页优先使用可直接照读的大白话流程：“客户发来消息 → 客户线看以前谈过什么 → 任务判断现在该做什么 → 草稿审批由 AI 先写、人确认后再发 → 审计记录过程与责任”。知识检索不硬塞进顺序步骤，而作为“先查公司资料、找不到依据就明确说明”的贯穿能力显示。
- 总览流程之后必须有可直接口述的“一句话总结”，不只依赖卡片让观众自行归纳。演讲页说明文字以投屏可读为准：主要说明 14–16px，流程节点与时间/来源不低于 12–13px；不得为了留白重新缩到 10–11px。渠道入口页沿用既有紧凑卡片，不受此放大规则影响。
- 规划能力统一显示“规划能力 / VISION”，不得把界面流程图写成已连接的 DeepSeek、真实企业知识库、跨设备会话或渠道回执。

## 可访问性与性能

可键盘操作，有 focus-visible；状态反馈使用 role=status；地球有说明文字/键盘交互；低动态模式不轮播问候、不自转、不强制平滑滚动。Canvas 限制 DPR 2；离屏与页面隐藏不做有效绘制；不使用上千 DOM 节点绘球。

验收：1440×960、390×844；中/英文；浅/深色；无水平溢出；未审核不可保存；刷新保存有效；编辑再审；用户可跳过入口。未来增加 Safari/Firefox、窄屏320px、高缩放与真实低端设备性能测量。
